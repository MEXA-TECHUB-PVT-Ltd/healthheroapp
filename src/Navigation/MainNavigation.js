import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MainApplication = () => {
  return (
    <View>
      <Text>MainApplication</Text>
    </View>
  );
};

export default MainApplication;

const styles = StyleSheet.create({});
